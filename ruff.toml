line-length = 88
target-version = "py311"

[lint]
select = ["E", "F", "W", "I"]

[lint.per-file-ignores]
"tests/*" = ["E501"]
# The SQL literals run past 88 columns; wrapping them splits statements
# mid-clause.
"store/db.py" = ["E501"]
