# Copy to .env if a default port is already taken on your machine.
# Defaults match the URLs in the README, so a clean machine needs no .env.
PANELIST_WEB_PORT=3000
PANELIST_API_PORT=8000
PANELIST_DB_PORT=55432
